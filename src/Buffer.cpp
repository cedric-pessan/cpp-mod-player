
#include "Buffer.hpp"

namespace mods
{
   /*Buffer::Buffer(u8* buf, callback freeCallback)
     : _buf(buf),
     _freeResourceCallback(freeCallback)
       {
       }
          
   
   Buffer::~Buffer()
     {
        _freeResourceCallback();
     }*/
}
