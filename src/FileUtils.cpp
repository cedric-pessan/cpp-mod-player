
#include "RBuffer.hpp"

namespace mods
{
   namespace FileUtils
     {
        RBuffer<u8> mapFile()
          {
             std::cout << "TODO: mapFile()" << std::endl;
             return RBuffer<u8>();
          }
     }
}
